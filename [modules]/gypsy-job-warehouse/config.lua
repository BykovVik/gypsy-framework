Config = {}

-- ====================================================================================
--                              ЛОКАЦИИ
-- ====================================================================================

-- Склад (тестовая локация - Legion Square)
Config.Warehouse = {
    coords = vector3(195.0, -933.0, 30.7),  -- Legion Square (центр города)
    heading = 90.0,
    blip = {
        sprite = 478,  -- Warehouse icon
        color = 5,     -- Yellow
        scale = 0.8,
        label = "Warehouse Job"
    }
}

-- ====================================================================================
--                              ТРАНСПОРТ
-- ====================================================================================

-- Нет транспорта (работа пешком)

-- ====================================================================================
--                              НАСТРОЙКИ РАБОТЫ
-- ====================================================================================

Config.Job = {
    MaxWorkers = 5,            -- Максимум рабочих одновременно
    BoxesPerShift = 10,        -- Ящиков за смену
    CooldownMinutes = 30,      -- Откат после смены (в минутах)
    CarryTime = 3,             -- Время переноски ящика (секунды)
    
    -- Время для нелегальных грузов
    IllegalHoursStart = 22,    -- Начало ночи (22:00)
    IllegalHoursEnd = 6,       -- Конец ночи (06:00)
    IllegalChance = 30         -- Шанс нелегала ночью (%)
}

Config.Payment = {
    -- Обычная погрузка (день)
    NormalPayment = 12,        -- $12 за ящик
    
    -- Нелегальная погрузка (ночь)
    IllegalPayment = 22        -- $22 за ящик
}

-- ====================================================================================
--                              ТОЧКИ РАБОТЫ
-- ====================================================================================

-- Склады с точками погрузки и разгрузки
Config.Warehouses = {
    -- Тестовая локация (Legion Square - центр города)
    {
        name = "Test Warehouse",
        location = vector3(195.0, -933.0, 30.7),        -- Legion Square (начало работы)
        loadPoint = vector3(200.0, -933.0, 30.7),       -- Точка взятия ящика (5м вправо)
        unloadPoint = vector3(190.0, -933.0, 30.7),     -- Точка разгрузки (5м влево)
        heading = 90.0
    }
}

-- Пропы для ящиков
Config.Props = {
    normal = 'prop_cs_cardbox_01',      -- Обычный ящик
    illegal = 'hei_prop_heist_box'      -- Подозрительный ящик (нелегал)
}

-- Анимации
Config.Animations = {
    carry = {
        dict = 'anim@heists@box_carry@',
        anim = 'idle',
        flag = 49
    }
}
