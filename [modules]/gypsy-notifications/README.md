# Gypsy Notifications - API Documentation

Модуль централизованных уведомлений для Gypsy Framework с ретро-футуристическим дизайном.

## Установка

1. Модуль уже находится в `[gypsy-framework]/[modules]/gypsy-notifications`
2. Добавьте в `server.cfg`:
```ini
ensure gypsy-notifications
```

## Использование

### Клиентская сторона

#### Экспорты

```lua
-- Базовый метод
exports['gypsy-notifications']:Notify(message, type, duration)

-- Короткие алиасы
exports['gypsy-notifications']:Success(message, duration)
exports['gypsy-notifications']:Error(message, duration)
exports['gypsy-notifications']:Warning(message, duration)
exports['gypsy-notifications']:Info(message, duration)
```

#### Примеры

```lua
-- Success уведомление
exports['gypsy-notifications']:Success('Покупка успешна!', 3000)

-- Error уведомление
exports['gypsy-notifications']:Error('Недостаточно денег!', 5000)

-- Warning уведомление
exports['gypsy-notifications']:Warning('Низкий уровень топлива', 4000)

-- Info уведомление
exports['gypsy-notifications']:Info('Вы получили новый предмет', 3000)

-- Полный синтаксис
exports['gypsy-notifications']:Notify('Текст сообщения', 'success', 3000)
```

### Серверная сторона

#### Через NotificationService (рекомендуется)

```lua
-- Получаем сервис
local NotificationService = exports['gypsy-core']:GetService('Notification')

-- Отправляем уведомление игроку
NotificationService.Send(source, 'Сообщение', 'success', 3000)

-- Отправляем всем игрокам
NotificationService.SendAll('Сообщение для всех', 'info', 5000)
```

#### Через события

```lua
-- Отправить конкретному игроку
TriggerClientEvent('gypsy-notifications:client:notify', source, {
    message = 'Текст уведомления',
    type = 'success', -- success, error, warning, info
    duration = 3000 -- опционально, по умолчанию 3000мс
})

-- Отправить всем игрокам
TriggerClientEvent('gypsy-notifications:client:notify', -1, {
    message = 'Сообщение для всех',
    type = 'info',
    duration = 5000
})
```

#### Примеры использования в модулях

```lua
-- В gypsy-bank при недостаточных средствах
local NotificationService = exports['gypsy-core']:GetService('Notification')
NotificationService.Send(source, 'Недостаточно средств на счёте', 'error', 5000)

-- В gypsy-inventory при полном инвентаре
TriggerClientEvent('gypsy-notifications:client:notify', source, {
    message = 'Недостаточно места в инвентаре',
    type = 'warning',
    duration = 4000
})

-- В gypsy-garage при успешной парковке
exports['gypsy-core']:GetService('Notification').Send(source, 'Транспорт припаркован', 'success')
```

## Типы уведомлений

| Тип | Цвет | Использование |
|-----|------|---------------|
| `success` | Зелёный (#00ff88) | Успешные операции |
| `error` | Оранжевый (#ff6600) | Ошибки, недостаток ресурсов |
| `warning` | Жёлтый (#ffb000) | Предупреждения |
| `info` | Голубой (#75bfcc) | Информационные сообщения |

## Конфигурация

Настройки находятся в `config.lua`:

```lua
Config.Position = 'top-right' -- Позиция на экране
Config.DefaultDuration = 3000 -- Длительность по умолчанию (мс)
Config.MaxNotifications = 5 -- Максимум одновременных уведомлений
Config.EnableSounds = false -- Звуковые эффекты
```

## Команды для тестирования

```lua
-- Тест конкретного типа
/testnotify success
/testnotify error
/testnotify warning
/testnotify info

-- Спам-тест (проверка очереди)
/spamnotify 10

-- Проверка регистрации в Service Locator
/checkservice Notification
```

## Особенности дизайна

- ✨ Ретро-футуристический стиль в духе 80-х
- 🌟 Неоновое свечение с анимацией
- 📱 Полупрозрачный фон для видимости днём
- 🎭 Плавные анимации появления/исчезновения
- ⏱️ Прогресс-бар с таймером
- 📊 Автоматическая очередь уведомлений
- 🎨 Адаптивный дизайн

## Интеграция с другими модулями

Модуль автоматически интегрируется с `gypsy-core` через `NotificationService`. Все модули, использующие `NotificationService`, автоматически будут использовать новый UI.

### Обратная совместимость

Модуль также слушает событие `gypsy-hud:client:notify` для обратной совместимости со старыми модулями.
